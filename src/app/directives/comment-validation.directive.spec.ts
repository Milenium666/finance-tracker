import { CommentValidationDirective } from './comment-validation.directive';

describe('CommentValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentValidationDirective();
    expect(directive).toBeTruthy();
  });
});
